class ConversationEntity {
  final int id;
  final String title;
  final DateTime createdAt;

  const ConversationEntity({
    required this.id,
    required this.title,
    required this.createdAt,
  });
}
